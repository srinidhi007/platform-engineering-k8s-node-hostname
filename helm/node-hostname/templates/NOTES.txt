1. Get the Ingress address:
   kubectl get ingress {{ include "node-hostname.fullname" . }}

2. If using nip.io with the NGINX LB IP:
   export LB_IP=$(kubectl -n ingress-nginx get svc ingress-nginx-controller -o jsonpath='{.status.loadBalancer.ingress[0].ip}')
   echo "http://{{ .Values.ingress.host }} (Host header -> $LB_IP)"

